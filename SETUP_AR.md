# دليل الإعداد السريع - تطبيق Control

## 🎯 الخطوات المطلوبة

### 1️⃣ احصل على مفتاح Resend API

1. اذهب إلى [resend.com](https://resend.com)
2. سجل حساب مجاني (100 إيميل يومياً)
3. اذهب إلى [API Keys](https://resend.com/api-keys)
4. أنشئ مفتاح API جديد
5. انسخ المفتاح (يبدأ بـ `re_`)

### 2️⃣ ضع المفتاح في الملف

افتح ملف `.env` وضع مفتاح API الخاص بك:

```env
RESEND_API_KEY=re_abc123xyz...
```

### 3️⃣ شغّل التطبيق

تحتاج لفتح **نافذتين** في Terminal:

**النافذة الأولى - الواجهة:**

```bash
npm run dev
```

**النافذة الثانية - الخادم:**

```bash
npm run server
```

### 4️⃣ افتح التطبيق

- الواجهة: http://localhost:5173
- الخادم: http://localhost:3001

## ✨ المميزات

- ✅ **دخول غير محدود**: 100 إيميل يومياً (بدلاً من 3 في الساعة)
- ✅ **سرعة فائقة**: Resend مصمم للسرعة
- ✅ **إيميلات احترافية**: تصميم جميل للأكواد
- ✅ **قاعدة بيانات قوية**: كل بياناتك محفوظة في Supabase
- ✅ **بدون قيود مزعجة**: بسيط ومباشر

## 🔧 كيف يعمل؟

1. المستخدم يدخل الإيميل والاسم
2. التطبيق يولد كود من 6 أرقام
3. الكود يُرسل عبر Resend
4. المستخدم يدخل الكود للتحقق
5. الملف الشخصي يُنشأ أو يُحمّل من Supabase
6. البيانات تُحفظ في قاعدة البيانات

## 🆘 حل المشاكل

**الإيميلات لا تُرسل؟**

- تأكد من مفتاح API في ملف `.env`
- تأكد أن الخادم يعمل (`npm run server`)
- راجع رسائل الخطأ في Terminal

**لا أستطيع الدخول؟**

- تأكد أن الخادمين يعملان (الواجهة + API)
- افحص Console في المتصفح
- تأكد من إدخال الكود الصحيح

---

صُنع بـ ❤️ من أجل البساطة والسرعة
