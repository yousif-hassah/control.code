# 🔧 إصلاح أخطاء Supabase - دليل كامل

## المشكلة:

التطبيق يظهر أخطاء 400 و 406 و 409 لأن جداول Supabase غير موجودة أو غير مهيأة بشكل صحيح.

## الحل (5 دقائق):

### الخطوة 1: افتح Supabase Dashboard

1. اذهب إلى: https://supabase.com/dashboard
2. اختر مشروعك
3. من القائمة الجانبية، اختر **SQL Editor**

### الخطوة 2: نفّذ السكريبت

1. اضغط **"New Query"**
2. افتح ملف `SUPABASE_COMPLETE_SETUP.sql` من المشروع
3. **انسخ كل المحتوى** (Ctrl+A ثم Ctrl+C)
4. **الصقه** في SQL Editor في Supabase
5. اضغط **"Run"** أو **"Execute"**

### الخطوة 3: تحقق من النجاح

بعد تنفيذ السكريبت، يجب أن ترى رسالة نجاح.

للتأكد، شغّل هذا الاستعلام:

```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public';
```

يجب أن ترى:

- ✅ profiles
- ✅ journals
- ✅ todos
- ✅ pinned_notes
- ✅ groups
- ✅ group_members
- ✅ group_messages

---

## ما الذي يفعله السكريبت؟

### 1. إنشاء الجداول:

- **profiles**: ملفات المستخدمين
- **journals**: المذكرات اليومية
- **todos**: قوائم المهام
- **pinned_notes**: الملاحظات المثبتة
- **groups**: المجموعات
- **group_members**: أعضاء المجموعات
- **group_messages**: رسائل المجموعات

### 2. إضافة Indexes:

لتحسين سرعة الاستعلامات

### 3. تفعيل Row Level Security (RLS):

لحماية البيانات (حالياً مفتوح للجميع للتطوير)

### 4. تفعيل Realtime:

للدردشة الجماعية المباشرة

---

## بعد تنفيذ السكريبت:

1. **أعد تحميل التطبيق** (F5)
2. **سجّل دخول** بإيميلك
3. **جرّب إنشاء مجموعة**:
   - اضغط على أيقونة المجموعات (الزر الأوسط)
   - اضغط "إنشاء مجموعة جديدة"
   - أدخل اسم المجموعة
   - احفظ الكود الذي يظهر
4. **جرّب الانضمام من حساب آخر**:
   - سجّل دخول بإيميل مختلف
   - اضغط "انضم لمجموعة"
   - أدخل الكود
   - الآن يمكنكم التحدث!

---

## إذا استمرت المشاكل:

### خطأ 400:

- تأكد من تنفيذ السكريبت كاملاً
- تحقق من أن جميع الجداول موجودة

### خطأ 406:

- تأكد من تفعيل RLS policies
- راجع السكريبت وتأكد من تنفيذ قسم RLS

### خطأ 409:

- هذا طبيعي عند محاولة إنشاء ملف موجود
- لا داعي للقلق

---

## ملاحظات أمان:

⚠️ **مهم**: السكريبت الحالي يسمح بالوصول الكامل للجميع (للتطوير).

**للإنتاج**، يجب تعديل RLS policies لتكون أكثر أماناً:

```sql
-- مثال: السماح للمستخدم برؤية مذكراته فقط
CREATE POLICY "Users can view own journals"
ON public.journals
FOR SELECT
USING (auth.uid() = user_id);
```

لكن حالياً، نحن نستخدم نظام مخصص للمصادقة (Nodemailer OTP)، لذا RLS مفتوح.

---

✅ **بعد تنفيذ هذه الخطوات، ستعمل جميع الميزات بشكل كامل!**

🎉 **الآن يمكنك إنشاء مجموعات والدردشة مع الأصدقاء!**
