# 🗄️ إعداد جداول المجموعات في Supabase

## ✅ التطبيق يعمل الآن!

لكن لإكمال ميزة المجموعات، يجب إنشاء الجداول في Supabase.

---

## 📋 الخطوات:

### 1️⃣ افتح Supabase Dashboard

اذهب إلى: https://supabase.com/dashboard

### 2️⃣ اختر مشروعك

اختر المشروع الخاص بك

### 3️⃣ افتح SQL Editor

من القائمة الجانبية، اختر **SQL Editor**

### 4️⃣ انسخ والصق السكريبت

1. افتح ملف `SUPABASE_GROUPS_SETUP.sql`
2. انسخ كل المحتوى
3. الصقه في SQL Editor
4. اضغط **Run** (أو `Ctrl + Enter`)

### 5️⃣ تأكد من النجاح

يجب أن ترى رسالة "Success" ✅

---

## 📊 الجداول التي سيتم إنشاؤها:

### 1. `groups` - جدول المجموعات

- `id`: معرف المجموعة (UUID)
- `name`: اسم المجموعة
- `code`: كود الانضمام (6 أحرف، فريد)
- `created_by`: معرف المنشئ
- `created_at`: تاريخ الإنشاء

### 2. `group_members` - جدول الأعضاء

- `id`: معرف العضوية
- `group_id`: معرف المجموعة
- `user_id`: معرف المستخدم
- `role`: الدور ('admin' أو 'member')
- `joined_at`: تاريخ الانضمام

### 3. `group_messages` - جدول الرسائل

- `id`: معرف الرسالة
- `group_id`: معرف المجموعة
- `user_id`: معرف المرسل
- `message`: نص الرسالة
- `created_at`: تاريخ الإرسال

---

## 🔒 الأمان (RLS):

السكريبت يتضمن:

- ✅ Row Level Security مفعّل
- ✅ سياسات أمان للقراءة والكتابة
- ✅ الأعضاء فقط يرون رسائل مجموعاتهم
- ✅ Real-time مفعّل للرسائل

---

## 🚀 بعد التنفيذ:

1. أعد تحميل التطبيق (`Ctrl + Shift + R`)
2. جرّب إنشاء مجموعة جديدة
3. انسخ الكود وشاركه
4. انضم من حساب آخر
5. ابدأ الدردشة! 💬

---

## ⚠️ ملاحظات:

- إذا كانت الجداول موجودة مسبقاً، السكريبت لن يحذفها
- إذا كنت تريد إعادة إنشاء الجداول، احذفها أولاً من Table Editor
- تأكد من أن `profiles` table موجود (للـ user names)

---

**بعد تنفيذ السكريبت، ستعمل ميزة المجموعات بالكامل!** 🎉
